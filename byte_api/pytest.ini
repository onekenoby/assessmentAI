[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: richiede il Database A reale e variabili ambiente configurate
    destructive: inserisce realmente BYTEA, documenti, contesti o job nel Database A
