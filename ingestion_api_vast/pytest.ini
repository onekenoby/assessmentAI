[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: richiede servizi reali e variabili ambiente configurate
    destructive: può reclamare ed elaborare un job reale dal Database A
